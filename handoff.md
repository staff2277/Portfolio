# Handoff Notes — Portfolio Project

> **Purpose:** This file is the running log for this project. Any model (or human) picking up work here should read this first, then update it before finishing a session. Keep entries short, factual, and dated — this is a handoff doc, not a changelog for users.

## How to use this file
- **Before starting work:** read "Project Overview" + the most recent 2-3 entries in "Change Log".
- **After making a change:** add a new entry at the top of "Change Log" (newest first) with date, what changed, why, and anything the next person needs to know (gotchas, open questions, follow-ups).
- **When you research something** (a library quirk, a Next.js 16 breaking change, a Three.js gotcha, etc.) that isn't obvious from the code, record it under "Research Notes" so nobody has to re-discover it.
- Keep it factual — no speculation presented as fact. If something is unresolved, say so under "Open Issues".

---

## Project Overview

- **Stack:** Next.js 16.2.1 (App Router), React 19.2.4, Tailwind CSS 4, React Three Fiber 9 / drei / postprocessing, Three.js 0.183, GSAP, Framer Motion, Lenis (smooth scroll), Leva (debug controls), Lottie.
- **Location:** `D:\Dev\Portfolio`
- **Structure:**
  - `app/` — routes: `page.js` (home), `work/`, `contact/`, `layout.js`, `globals.css`
  - `components/` — `Navbar.js`, `PageTransitionProvider.js`, `SmoothScroll.js`
  - `components/3d/` — `HeroCanvas.js`, `HeroScene.js`, `SphereShell.js`, `useCameraSequence.js` (the R3F hero scene)
  - `.agents/mcp.json` — MCP config for agent tooling
  - `AGENTS.md` / `CLAUDE.md` — **important**: this project pins a Next.js version with breaking changes vs. what's in most models' training data. Read `node_modules/next/dist/docs/` before writing Next.js-specific code (routing, config, data fetching, etc.) rather than assuming familiar APIs still apply.
- **Scripts:** `npm run dev` / `build` / `start` / `lint`

## Conventions / Things to Know
- This repo intentionally overrides default model assumptions about Next.js -- always check local docs (`node_modules/next/dist/docs/`) before relying on memorized Next.js behavior, especially for App Router, config, or data-fetching APIs.
- 3D work lives under `components/3d/`; the hero scene (`HeroScene.js` + `SphereShell.js` + `HeroCanvas.js`) is the main R3F surface. As of 2026-07-31, there is **no scroll-driven camera animation** -- `Camera_Export` sits statically at frame 0 (baked clip loaded via `AnimationMixer`, held with `mixer.setTime(0)`). No screen loader, no page-transition overlay either. All three are ready to be rebuilt from scratch; see the 2026-07-31 (3) Change Log entry for exactly what was removed and why.
- `.archive/` at the project root holds old implementations moved aside rather than deleted (the Filesystem MCP tooling used in these sessions has no delete capability). Not part of the active app -- nothing imports from it. Safe to delete by hand, or via a tool with real file-delete access (e.g. Claude Code), once you're sure you don't need to reference the old code.
- No test suite currently present.

## Open Issues
- `heroSectionRef` and `spacerRef` are still threaded `app/page.js` → `HeroCanvas.js` → `HeroScene.js`, but as of 2026-07-31 (3) nothing reads them there anymore (their only consumer, `useCameraSequence.js`, was moved to `.archive/`). Left in place since a new scroll implementation will likely want `heroSectionRef` again (it's the pin target) and possibly `spacerRef`; harmless no-ops until then.

## Research Notes
- **2026-07-31 (2) — Camera_Export baked animation, actual keyframe analysis.** User asked whether the camera moves forward in a straight line on Y, then does a quarter-circle while rising. Parsed `showroom.glb`'s raw keyframe buffer directly (111 keyframes, `animations[0]` = "Camera_ExportAction", targets node 2 = `Camera_Export`, LINEAR interpolation, `translation` + `rotation` channels, duration 4.5833s = 110 frames @ 24fps -- matches `HERO_START`/`HERO_HOLD`/`HERO_END` in `HeroScene.js`). Axis convention: glTF/this export is Y-up, X-right; camera looks down -Z locally.
  - **Frames 0→42 (Phase A range)**: straight line confirmed -- max perpendicular deviation from the start→end line across all keyframes in this range is 0.025 units over an 85.5-unit path (effectively exact). BUT the dominant axis is **Z, not Y**: position goes `(-0.15, 14.90, 115.29)` → `(-0.07, 14.08, 29.73)`. ΔZ = -85.5 (dolly forward), ΔX = +0.08 (~flat), ΔY = -0.82 (slight *drift down*, not up). If "Y axis forward" meant the depth/forward axis colloquially, this holds; if it meant the literal glTF Y (vertical) axis, it does not -- Y is nearly flat here.
  - **Frames 42→110 (Phase B range)**: NOT a quarter circle. Least-squares circle fit to the X-Z path: center ≈ `(2.87, 4.99)`, radius ≈ 19.7. Angular sweep from the fit center goes 96.8° → -99.0°, a **~196° sweep** (more than a half circle, not a quarter). Radius isn't constant either -- ranges ~17.5 to ~26 across the sweep (tightens from 26→17.5 through roughly the first two-thirds, then holds steady ≈ 17.5–21), so it's an eased/slightly-spiraling arc rather than a geometrically perfect circle. Y **does** rise continuously and monotonically the whole time (14.08 → 56.92, +42.8 total) confirming "moving up", but the rise accelerates hard in the back third -- roughly +16.6 over frames 42→90, then +26.2 over just frames 90→110 (ease-in climb, not linear).
  - Full per-frame table (frame, X, Y, Z, fit-radius, fit-angle) generated during this analysis but not saved to disk -- rerun the parse (see method below) if the exact numbers are needed again.
  - **Method** (for reuse, since the Filesystem MCP struggled with this file before): `Filesystem:copy_file_user_to_claude` succeeded this time (11MB, prior attempts had timed out -- seems intermittent/transient, not a hard blocker). Once copied into Claude's own sandbox at `/mnt/user-data/uploads/showroom.glb`, parsed with a plain Python `struct`/`json` GLB reader (12-byte header, JSON chunk, BIN chunk; accessors 8/9/10 = time/translation/rotation for `Camera_ExportAction`). Both Blender MCP tools (`execute_blender_code` live-connection and `execute_blender_code_for_cli`) and the Puppeteer MCP tool were unresponsive/timed out throughout this session and were not usable as alternatives.
  - Side note: node 4 ("Empty") also has an animated translation (`EmptyAction`, accessor 11) in the same glTF animations array, but it's static until roughly frame 90-100 and then only rises in Y (X/Z constant) -- doesn't look like a look-at/aim target for the camera's arc. Not investigated further since it wasn't what was asked; flagging in case it matters later (could be a light or reference empty).

- **2026-07-31 — Verified: no autoplay exists in the baked camera animation.** Reviewed `HeroCanvas.js`, `HeroScene.js`, and `useCameraSequence.js` line by line looking for anything that free-runs `Camera_Export`'s baked clip on load. Found none:
  - `HeroScene.js` creates the mixer with `action.play(); action.paused = true;` immediately — it's a scrub target, never a playing action.
  - `useCameraSequence.js` Phase A (frames 0→`HERO_HOLD`) only advances `progress` inside `wheel`/`touchmove` handlers — no rAF ticker increments it on its own.
  - Phase B (`HERO_HOLD`→`HERO_END`) is a GSAP `ScrollTrigger` whose `onUpdate` (which calls `mixer.setTime()`) only fires on actual scroll.
  - `HeroCanvas.js` never calls `mixer.update(delta)`, so nothing is advancing the clip's internal clock either.
  - Confirmed with the user (2026-07-31) that no autoplay is visible in the running dev server — this matches the code. **Conclusion: the "remove autoplay animation" task is already satisfied by the current code; no changes were needed.** If autoplay-like behavior is ever reported again, suspect a regression in one of these three files rather than assuming it was never fixed.
  - Side note: briefly misidentified the target during investigation as `SphereShell.js`'s idle "heartbeat" pulse (a `performance.now()`-driven core scale/color/light pulse + idle vertex wobble, ported faithfully from `public/models/sphere_animation.js`). That pulse **is** a self-running, non-scroll animation, but the user clarified it is NOT what "autoplay animation" refers to in this project and it must be left untouched. Do not remove or alter it under this task.

---

## Change Log

### 2026-08-14 — Implemented Interactive Magnetic & Bulge SVG SideProgressBar
Implemented complete [`SideProgressBar.js`](file:///d:/Dev/Portfolio/components/SideProgressBar.js) component mounted in [`app/layout.js`](file:///d:/Dev/Portfolio/app/layout.js):
- **Magnetic Ball Rest State:** Physics-based spring pull ($\Delta X, \Delta Y = 0.35 \times \text{delta}$) with elastic snap-back.
- **Morph Expansion:** Click trigger warps ball into a vertical progress navigation bar with close/collapse trigger.
- **Dynamic Organic Bulge:** SVG `<path>` bezier curve morphing driven by GSAP (`elastic.out(1, 0.4)`) dynamically projecting outward around only the hovered link.
- **Scroll Progress & Nav Sync:** Active indicator tracer line driven by scroll percentage $\frac{\text{window.scrollY}}{\text{doc.scrollHeight} - \text{win.innerHeight}} \times 100$.

### 2026-08-14 — Fixed build error: removed missing Navbar import in app/layout.js
Fixed `Module not found: Can't resolve '../components/Navbar'` error in [app/layout.js](file:///d:/Dev/Portfolio/app/layout.js). `Navbar.js` had been previously refactored or deleted, but `app/layout.js` was still attempting to import `<Navbar />`. Removed the unused import and component usage from `app/layout.js`.

### 2026-08-01 (6) — Spotlight: hardcoded position instead of tracking sphereTransform
User clarified the spotlight request: hardcode the sphere's actual position (Blender `0, 0, 14.31` -> loaded Y-up scene `0, 14.31, 0`) as a literal `SPHERE_POSITION` constant rather than reading it live from `sphereTransform.position`, add the height offset on top of that, and aim straight down at that same hardcoded spot. Changed in `HeroScene.js`: `SPHERE_POSITION` is now a module-level `THREE.Vector3(0, 14.31, 0)`; the spotlight and its target both use it directly and no longer depend on `sphereTransform` at all, so they render unconditionally rather than gated behind `{sphereTransform && ...}`. `SphereShell` is unchanged -- still uses the live `sphereTransform.position`.

Also noted: the live file's `angle`/`intensity` values (`Math.PI / 4`, `5000`) differed slightly from what was written in the 2026-08-01 (5) entry (`Math.PI / 8`, `800`) by the time this edit was made -- left as-is since the user didn't ask about those specifically, but flagging in case of another concurrent edit (see the 2026-08-01 (1)/(2) entries for the earlier instance of this happening).

### 2026-08-01 (5) — Confirmed fix working; removed HDRI, added spotlight above sphere
User confirmed (after a full dev-server restart + hard refresh, not just Fast Refresh) that the 2026-08-01 (4) fix works -- **the camera animation now plays correctly on scroll.** The earlier "still broken" report after that fix was Fast Refresh not rebuilding the mixer/action instance; a full restart was needed since the change was inside a `useEffect` body, not something Fast Refresh reliably re-triggers on its own.

Two further changes in `HeroScene.js`:
- **Removed the HDRI entirely** -- deleted the `HDRLoader` import/load effect, the `hdriTexture` state, and the effect that set `threeScene.background`/`threeScene.environment`. Scene background/environment are back to Three.js defaults (null).
- **Added a spotlight above the sphere** -- positioned at the sphere's X/Z with a `SPOTLIGHT_HEIGHT_OFFSET` of 120 units added to its vertical component, aimed down at the sphere via a `target` object explicitly added to the scene graph (`SpotLight.target` only orients correctly if the target is part of the scene graph -- setting `target.position` alone without adding it via `<primitive>` would not work). Tunable: `angle`, `penumbra`, `intensity`, `decay` are first-pass values, not verified against the actual render yet (no browser access).
- **Axis note for future reference:** the user asked for the spotlight higher "on the Z axis," using Blender's Z-up convention. Since this project exports `export_yup=True`, the loaded Three.js scene is Y-up -- the sphere's Blender Z position (~14.3) became its Y position in this scene. The height offset was applied to the Y component of the loaded position, not Z, so it actually reads as "higher" visually. Worth flagging for any future "axis" instruction from the user: they think in Blender's Z-up frame, but everything in `HeroScene.js`/`SphereShell.js` operates in the post-conversion Y-up frame.

**Not yet verified in the browser by me** (no browser access) -- user should confirm the spotlight looks right (height, cone angle, intensity) and that removing the HDRI didn't leave the sphere/scene too dark given `SphereShell` may have relied on environment reflections.

### 2026-08-01 (4) — Root cause found: paused AnimationAction silently ignores mixer.setTime()
User reported the new scroll hook (2026-08-01 (3)) still didn't move the camera, but with `[cameraScroll]` console logs showing `progress`/`frame` correctly sweeping 0->110 as they scrolled, and `mixer`/`camera` both truthy -- i.e. the scroll-to-frame math was provably correct, so the bug had to be in how that frame value gets applied.

**Root cause:** `HeroScene.js`'s mixer setup had `action.paused = true` (present since the very first version of this project, carried through every iteration so far -- never actually the bug until now, or more likely always the bug and never isolated before). This is a real Three.js `AnimationAction` internals quirk: `mixer.setTime(t)` resets the action's internal `time` to 0 and then calls `update(t)`, which calls `_updateTimeScale()` -- and that function returns `0` whenever the action is `paused`, which zeroes the delta applied to the clip. Net effect: **a paused action's `setTime()` always evaluates to clip time 0, no matter what value is passed in.** The camera silently never advances past frame 0, while everything upstream (scroll listener, progress math, `mixer.setTime()` call itself) works perfectly -- exactly matching the reported symptom.

**Fix:** removed `action.paused = true` from the mixer setup effect in `HeroScene.js`. Safe to leave unpaused because nothing in this codebase calls `mixer.update()` in a continuous loop (a manually-constructed `AnimationMixer` isn't auto-driven by R3F the way `drei`'s `useAnimations` hook would; only this project's own `mixer.setTime()` calls ever touch it), so there's no risk of the clip "playing" on its own once unpaused.

**Not yet re-verified in the browser by me** (no browser access). This should be the real fix, but user should hard refresh and confirm the camera actually visibly moves now, not just that the console numbers look right (which they already were).

### 2026-08-01 (3) — New Blender animation baked/exported; new from-scratch scroll hook built
Two separate pieces of work this entry:

**1. Blender: removed old animation, built and baked a new one.** Per user request:
- Deleted the old baked `Camera_ExportAction` (and the `Camera_Export` object itself, deleted by the user directly in Blender) and the two timeline markers (`Hero Section`, `Hero Transition`).
- Cleared `NurbsPath`'s old `Animate Path` setup (`NurbsPath.001Action`, which had zero real keyframes anyway) and turned `use_path` off -- the Follow Path + Track To constraints on `Camera.001` were explicitly left in place (user confirmed: clear the animation, keep the constraints).
- User then built a new animation directly on `Camera.001`: keyframed the Follow Path constraint's `offset` property (0 -> -42 -> -100 across frames 0/50/110) while Track To keeps the camera aimed at `Empty` (which itself has a small animation -- static in X/Z, rises in Y from frame ~85 to 110). `frame_start`/`frame_end` are 0/110 @ 24fps, unchanged.
- Baked this: duplicated `Camera.001` -> renamed the duplicate to `Camera_Export` -> `bpy.ops.nla.bake(visual_keying=True, clear_constraints=True, only_selected=True)` across frames 0-110 -> renamed the resulting action to `Camera_ExportAction`. **`Camera.001` itself was left untouched** (still has its live constraints + the 3-keyframe offset animation) -- `Camera_Export` is a separate, self-contained, constraint-free baked copy purely for export. Verified independently (outside Blender, parsing the glb) that the bake produced real, substantial motion (~137 units of travel frame 0->110), not a static/degenerate result.
- Exported to `public/models/showroom.glb` via `bpy.ops.export_scene.gltf(export_format='GLB', export_animations=True, export_cameras=True, export_draco_mesh_compression_enable=True)` -- same settings as the original export.
- **Important gotcha for next time:** the glb now contains **three** animation clips, not one -- `gltf.animations` is `[Camera.001Action, EmptyAction, Camera_ExportAction]` in that order. Blender's glTF exporter auto-bakes `Camera.001`'s own constraint-driven motion for export too (since glTF has no constraint concept), so it ends up with its own translation/rotation track alongside the deliberately-baked `Camera_Export`. **Any code reading `gltf.animations[0]` assuming it's the camera clip is now wrong** -- must find the clip by name (`"Camera_ExportAction"`). This bit `HeroScene.js` (see part 2 below) since it still had the old index-based lookup from before this re-export.
- Not re-verified end-to-end in the browser by me after this bake (no browser access) -- covered by part 2's testing ask.

**2. Built a new, deliberately simpler scroll-driven camera hook from scratch.** New file: `components/3d/useCameraScroll.js` (exports `useCameraSequence` -- kept the same export name so `HeroScene.js`'s call site reads naturally, but it's a new implementation, not a restore of the old one). Key design decision: **single continuous GSAP ScrollTrigger scrub across the whole clip (frames 0->110), no wheel-jack phase, no phase handoff, no autoplay, no parallax.** After several rounds this session debugging a two-phase (wheel-jack + ScrollTrigger) version's handoff/momentum/pinSpacing bugs (see the 2026-08-01 (1)/(2) entries above and the 2026-07-31 entries below), removing the handoff entirely removes that whole class of bug rather than patching it further. Specifics:
- `pinSpacing` left at GSAP's default (`true`) -- same lesson as before, don't try to hand-manage pin spacing.
- Scroll distance is `SCROLL_DISTANCE = "+=300vh"`, a tunable constant with no authorial backing (the old Hero Section/Transition markers that used to inform this are gone from Blender) -- treat as a starting guess, adjust to taste.
- `mixer.setTime()` driven purely by `self.progress` mapped linearly from `startFrame` (0) to `endFrame` (110) via `gsap.utils.mapRange` -- no separate hold/curve split since there's no marker distinguishing them anymore.
- Kept the same `DEBUG`/`debugLog` pattern as the old hook (throttled `console.log` prefixed `[cameraScroll]`) for the same reason: get real numbers from a live session instead of guessing. **Remove once confirmed working.**
- `HeroScene.js` updated: fixed the `gltf.animations[0]` bug described in part 1 (now finds `Camera_ExportAction` by name, falling back to `[0]` only if not found), removed the stale `HERO_HOLD` export (no longer meaningful), added `CAMERA_CLIP_NAME`/`FPS` consts, wired in the new hook.
- `spacerRef` removed everywhere it was threaded through (`app/page.js` -> `HeroCanvas.js` -> `HeroScene.js`) since the new hook doesn't use a manual spacer (relies on GSAP's automatic pinSpacing, same lesson as before). `heroSectionRef` is still threaded through and is the only ref the new hook needs.
- `SmoothScroll.js` comments updated (no code change) -- no longer describes a Phase A wheel-jack that doesn't exist anymore.

**Not yet verified by me:** whether this actually produces the intended smooth camera scrub in the browser (no execute/browser access from this session). User should restart the dev server, hard refresh, open devtools console, and scroll through the hero section, then report the `[cameraScroll]` log output plus what's visually observed. If the `SCROLL_DISTANCE` feels too fast/slow, that's the one constant to tune first.

### 2026-08-01 (2) — Rolled back the previous reconnection; `.archive/` confirmed reference-only
User clarified the 2026-08-01 (1) entry below was a misstep: `.archive/` is reference material only, and the actual intent was to implement the scroll-driven camera animation **from scratch**, not restore the old hook. Rolled back:

- `HeroScene.js`: removed the `import { useCameraSequence } from "./useCameraSequence"` and the hook call added in (1). Restored the mixer effect to hold `Camera_Export` statically at frame 0, with comments pointing at `.archive/` as reference-only.
- `components/3d/useCameraSequence.js` (the live copy re-created in (1)): moved to `.archive/useCameraSequence.duplicate-2026-08-01.js` via `Filesystem:move_file` (this session does have move, just not delete). Content is identical to `.archive/useCameraSequence.js`, so nothing new to learn from it -- kept only so no work is silently lost; safe to actually delete either archive copy by hand or via a tool with real delete access.
- `app/page.js` was **not** reverted -- the manual 250vh spacer `<div>` removed in an earlier round is still gone, and the comment there still references the old `useCameraSequence.js` pinSpacing approach. Whoever implements the new scroll animation should treat `app/page.js`'s current state (no manual spacer, `heroSectionRef`/`spacerRef` still threaded through from `HeroCanvas.js` → `HeroScene.js` but unused) as the starting point, not as something tied to the old implementation.
- Current live state: `HeroScene.js` has zero scroll-driven behavior. `Camera_Export` is bound, held at frame 0, and set as the active R3F camera -- that's the full extent of it. `SmoothScroll.js`/Lenis is present in `app/layout.js` but has no consumers (`useLenis()` isn't called anywhere in the live tree). This is the actual, confirmed-correct starting point for a from-scratch implementation.
- Everything under **Research Notes** below (the glb/Blender keyframe verification, the axis analysis, the confirmation that Three.js's `AnimationMixer`/quaternion slerp interpolates the baked clip correctly) remains valid and reusable for the new implementation -- none of that was specific to the old hook's code, just to the underlying animation data.

### 2026-08-01 (1) — Reconnected useCameraSequence.js; documented a live concurrent-session conflict
**Superseded by (2) above -- left in place as a record, but do not act on the "resolution" described here.**
**Context:** a separate session working on this same project concurrently with this one (see 2026-07-31 (3) entry below) had archived `useCameraSequence.js` and removed its usage from `HeroScene.js`, intending a from-scratch rebuild. This session had spent the whole conversation fixing that exact hook (unaware it had been disconnected), which is why every fix appeared to have no visible effect when tested -- `HeroScene.js` wasn't calling it at all, so the camera sat frozen at `mixer.setTime(0)` regardless of what the hook did. Confirmed live mid-session: the file was still present and editable early in this conversation, then found moved to `.archive/` a few turns later with no action taken by this session -- i.e. the other session's edit landed while this one was in progress.

**Resolution (user confirmed no other session was intentionally active, asked to continue from current state):** restored `.archive/useCameraSequence.js` (which already contained every fix made in this session -- nothing was lost) to `components/3d/useCameraSequence.js`, and re-added `import { useCameraSequence } from "./useCameraSequence"` + the hook call in `HeroScene.js`, passing `mixerRef`, `cameraObjectRef: cameraObjRef`, `fps: 24`, `holdFrame: HERO_HOLD`, `endFrame: HERO_END`, `heroSectionRef`, `spacerRef`. Dropped the `disabled: !revealed` gate that used to tie this to `PageTransitionProvider` (also archived in the 2026-07-31 (3) entry, and not restored) -- `disabled` now just defaults to `false`, so Phase A goes live as soon as `HeroScene` mounts.

**Fixes carried over in the restored hook** (all made earlier in this same session, prior to the archive event):
- `pinSpacing: false` on the Phase B `ScrollTrigger` removed (left at default `true`); `end` changed from a `spacerRef.current.offsetHeight` measurement to a fixed `"+=250vh"`. The old config let pinning collapse the layout by the hero's own height with nothing compensating, which threw off start/end recalculation on `invalidateOnRefresh` and produced an erratic, non-monotonic progress ("camera spins weirdly").
- Removed the now-redundant manual spacer `<div>` in `app/page.js` (GSAP's own pinSpacing reserves that room now; keeping both would double the gap).
- Loosened wheel-nudge tuning (`NUDGE_SCALE`/`NUDGE_CLAMP`) -- a single normal mouse-wheel tick previously could only add ~0.035 progress, requiring ~29 ticks to clear Phase A; now ~8-9.
- Added a 350ms momentum-swallow window in `endPhaseA()`: wheel/touch listeners now stay attached (but inert, since `nudge()` no-ops once phase flips) for 350ms after Phase A ends before removing them and starting Lenis, so trackpad inertial "coast" events don't flow straight into the freshly-started `ScrollTrigger` as one uncontrolled burst right at the handoff.
- Removed the autoplay system entirely (per explicit user request) -- Phase A `progress` now only ever changes inside `nudge()`, itself only called from real `wheel`/`touchmove` events. No idle timer, no background progression.
- Removed the mouse-parallax camera system entirely (per explicit user request) -- no more `pointermove` listener, damped yaw/pitch, or per-frame quaternion composition in Phase B. Phase B's `useFrame` branch is gone; `ScrollTrigger.onUpdate` calling `mixer.setTime()` is now the only thing driving Phase B.
- Left in place: `DEBUG`/`debugLog` temporary console logging at Phase A init, every `nudge()`, the Phase A->B handoff, `ScrollTrigger` creation, and every `onUpdate` tick. **Remove once the sequence is confirmed working end-to-end** (search for `TEMP DEBUG` in `useCameraSequence.js`).

**Verified separately, not the cause of anything above:**
- The exported `showroom.glb`'s `Camera_ExportAction` (111 keyframes, `translation` + `rotation` on `Camera_Export`, matches `HERO_START`/`HERO_HOLD`/`HERO_END`) was checked byte-for-byte against the live Blender file's fcurve data and matches exactly (accounting for the standard Z-up -> Y-up axis conversion). The motion is smooth and matches the intended design: frames 0-42 are a straight dolly forward (X/Z ~flat, Y-equivalent axis moves ~85 units), frames 42-110 curve out and rise (~43 units) with a natural inflection right at frame 42. **Do not re-bake or re-export** -- the data is already correct, and re-baking now would be actively destructive: the live Blender file's `Camera_Export` currently has no constraint driving it (the Follow Path + Track To setup the user built is on a different, unused object, `Camera.001`), so a fresh bake would capture a static pose and overwrite the good action.
- Confirmed Three.js's `AnimationMixer`/`QuaternionKeyframeTrack` interpolate this data correctly (proper slerp for the quaternion rotation track, not naive per-component lerp), so the animation model itself was never the problem.

**Not yet verified by me:** whether the reconnected hook now actually produces the intended smooth animation end-to-end in the browser (no execute/browser access from this session). User should restart the dev server, hard refresh, open devtools console, and scroll through the full sequence, then report back the `[cameraSequence]` log output plus what's visually observed.

**Flagging for whoever reads this next:** this session and the one described in the 2026-07-31 (3)/(2)/(1) entries below were evidently editing this same file concurrently at some point, with conflicting intents (incremental fix-forward vs. archive-and-rebuild-from-scratch). The user was not aware of a second active session when asked. If this happens again, treat any surprising file-state change (a file vanishing, an import disappearing, behavior reverting) as a signal to stop and check the live filesystem state before continuing, rather than assuming your own edit history is the full picture.

### 2026-07-31 (3) — Removed scroll animation, screen loader, and page transition
User asked to tear these out entirely to reimplement the scroll animation from scratch. Summary of what's gone and what's still there:

- **Scroll animation (Phase A/B camera sequence)**: `components/3d/useCameraSequence.js` moved to `.archive/useCameraSequence.js` (see note below on why "moved" not "deleted"). Its call site and import removed from `HeroScene.js`. The underlying `AnimationMixer`/`Camera_Export` clip plumbing was left in place and now just holds the camera statically at frame 0 (`mixer.setTime(0)`) — that's infra a new scroll implementation will still need, not "the scroll animation" itself.
- **Screen loader**: the black "Loading" overlay div in `HeroCanvas.js` removed. The underlying async gate (`ready = gpuReady && Boolean(assets)`) is untouched — the `<Canvas>` still doesn't mount until assets are ready, there's just nothing shown while it waits (blank/whatever the page background is). If you want a new loader, that's the place to add it.
- **Page transition**: `components/PageTransitionProvider.js` moved to `.archive/PageTransitionProvider.js`. Removed from `app/layout.js` (no longer wraps `{children}`). `HeroCanvas.js` no longer calls `usePageReady()`/`setReady`/`revealed` — all of that machinery is gone since it existed only to coordinate with the transition overlay and the (also removed) scroll-animation reveal gate from the previous session's fix.
- `SmoothScroll.js` / Lenis (`app/layout.js`) was **left in place** — it's general-purpose smooth-scroll infra for the whole site, not specific to the hero's scroll animation, and nothing indicated it should go too. It currently has no consumers (`useLenis()` was only called from the now-archived `useCameraSequence.js`), so it's inert but harmless until a new scroll implementation picks it up again.
- **Important — no delete capability**: the Filesystem MCP connector available in this session does not expose a delete-file tool, only read/write/move/list. So instead of deleting `useCameraSequence.js` and `PageTransitionProvider.js` outright, they were moved to a new `.archive/` folder at the project root (out of the module graph, so nothing imports them, but not permanently destroyed). If you want them gone for real, delete `D:\Dev\Portfolio\.archive\` by hand, or ask a session with real file-delete access (e.g. Claude Code) to do it.
- Files touched: `app/layout.js`, `components/3d/HeroCanvas.js`, `components/3d/HeroScene.js`. Files moved: `components/3d/useCameraSequence.js` → `.archive/`, `components/PageTransitionProvider.js` → `.archive/`. `SphereShell.js` not touched.
- Not tested against a live dev server by me (no execute access to the user's machine in this session). User should verify `npm run dev` still boots clean — in particular check for any leftover import errors, and that the hero renders with `Camera_Export` static at frame 0 with no console errors about missing modules.

### 2026-07-31 (2) — Fixed "scroll dead until a few tries" after reload
- **Root cause**: Phase A's wheel/touch listeners (and `lenis.stop()`) were being attached as soon as hero assets finished loading (`ready` in `HeroCanvas.js`), but the intro cover overlay in `PageTransitionProvider.js` takes ~1.25s (`delay: 0.15` + `duration: 1.1`) to visually wipe away after that same moment. Scroll input during that window was being consumed by Phase A (advancing `progressRef` silently) with nothing on screen to show for it — producing exactly the "scroll a few times before anything moves" symptom the user reported.
- **Fix**: added a `revealed` boolean to `PageTransitionProvider`'s context, set `true` only in the wipe animation's `onComplete` (i.e. once the overlay is actually gone, not just once it's started disappearing). Threaded it `HeroCanvas.js` → `HeroScene.js` → `useCameraSequence({ disabled: !revealed })`. Phase A's setup effect now only runs (attaches listeners, calls `lenis.stop()`) once the reveal has genuinely finished.
- Also added `mixer.setTime(0)` right after mixer/action creation in `HeroScene.js`, decoupled from the `revealed` gate, so `Camera_Export` is posed at the baked frame-0 transform immediately rather than sitting at its raw export transform until Phase A first activates.
- Updated the stale `disabled` JSDoc in `useCameraSequence.js` (previously said it was only for a since-removed OrbitControls debug view).
- Files touched: `components/PageTransitionProvider.js`, `components/3d/HeroCanvas.js`, `components/3d/HeroScene.js`, `components/3d/useCameraSequence.js`. `SphereShell.js` not touched.
- Not yet tested against a live dev server by me (no execute access to the user's machine in this session) — user should verify: reload, scroll immediately while the screen is still black, confirm no wasted/dead scrolling once the scene is revealed.

### 2026-07-31 — Investigated "remove autoplay animation" task; no code change needed
- Reviewed `HeroCanvas.js`, `HeroScene.js`, `useCameraSequence.js` for any auto-running camera animation on the baked `showroom.glb` clip (`Camera_Export`).
- Found the mixer is already paused-and-scrubbed-only (never free-playing), and both Phase A (wheel/touch) and Phase B (GSAP ScrollTrigger) only advance on real user input. No autoplay path exists.
- User confirmed no autoplay is visible in the dev server, just wanted confirmation it's gone. Treating this task as already satisfied — see Research Notes for the full trail.
- Along the way, briefly edited then fully reverted `SphereShell.js` (removed and restored its idle pulse/`HIGHLIGHT_COLOR` const) after misidentifying it as the autoplay target. User caught this and clarified the pulse is intentional and off-limits. Net diff on `SphereShell.js`: none — verified back to original.
- Attempted to inspect `showroom.glb`'s animation clips directly (via `Filesystem:copy_file_user_to_claude` and via Blender's `execute_blender_code_for_cli`) to rule out a second baked clip; both attempts failed (MCP copy timed out on the ~11MB file; Blender CLI tool couldn't find the `blender` executable — needs `BLENDER_PATH` env var set to use that tool from this project). Not blocking since the JS-side investigation was conclusive, but noting for next time this kind of inspection is needed.

### 2026-07-30 — Initial handoff doc created
- Created this `handoff.md` to track project changes and research going forward.
- No functional code changes made yet this session — this is just the scaffold.
- Reviewed current project state: Next.js 16 / React 19 / R3F portfolio site with a 3D hero scene, work and contact routes, smooth scroll and page transition providers. No test suite. `AGENTS.md` flags that this Next.js version diverges from training-data assumptions.
